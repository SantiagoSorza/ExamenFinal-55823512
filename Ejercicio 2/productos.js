const nombreProducto = document.getElementById("name").value;
const cartegoria = document.getElementById("cartegoria").value;
const precio = parseFloat(document.getElementById("precio").value);
const unidades  = parseInt(document.getElementById("unidades").value);
let descuento  = 0;

if (cartegoria == 'A'){
    switch (unidades){
        case (1>=10) {

        }
    }
}
    let precioTotal = precio * unidades;
    let valorDescuento = precioTotal - descuento
